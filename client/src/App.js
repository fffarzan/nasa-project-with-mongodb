import { BrowserRouter } from "react-router-dom";

import Default from "./pages/Default/Default";

export default () => {
  return (
    <BrowserRouter>
      <Default />
    </BrowserRouter>
  );
};
